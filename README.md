# GVSU Budget Visualization

Works on `apache2`.
![Screenshot](./screen.png "Main Page")

Heroku configuration in `Procfile` and `composer.json`. URLs in `.htaccess`.

Main project is [here](https://github.com/yarnaid/Visual-Town-Budget).